# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=hofflabs_dev
DATABASE=Hofflabs

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here

# Encryption Configuration
ENCRYPTION_KEY=your_32_character_encryption_key

# API Configuration
BACKEND_API_URL=http://localhost:3000

# SMTP Email Configuration
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_USER=your-email@domain.com
SMTP_PASS=your-email-password

# Test Email Configuration
# All test emails will be redirected to this address
TEST_EMAIL_RECIPIENT=your-test-email@yourdomain.com

# Vault Configuration (Optional)
VAULT_ADDR=http://localhost:8200
VAULT_PASS=your_vault_password

# Environment
NODE_ENV=development
TEST_MODE=false

# Security Configuration
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://yourdomain.com

# IMPORTANT: Change these in production!
# Generate strong random values for production use
